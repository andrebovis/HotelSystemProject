<h2>📅 Lista de Reservas</h2>
<table border="1" cellpadding="5">
  <tr>
    <th>ID</th>
    <th>Cliente ID</th>
    <th>Quarto ID</th>
    <th>Check-in</th>
    <th>Check-out</th>
  </tr>
  <% @reservas.each do |r| %>
    <tr>
      <td><%= r["id"] %></td>
      <td><%= r["cliente_id"] %></td>
      <td><%= r["quarto_id"] %></td>
      <td><%= r["data_checkin"] %></td>
      <td><%= r["data_checkout"] %></td>
    </tr>
  <% end %>
</table>
<a href="/">⬅ Voltar</a>
